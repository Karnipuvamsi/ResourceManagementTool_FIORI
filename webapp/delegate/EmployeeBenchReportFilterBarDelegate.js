

sap.ui.define([
    "glassboard/delegate/BaseFilterBarDelegate"
], function (BaseFilterBarDelegate) {
    "use strict";

    const EmployeeBenchReportFilterBarDelegate = Object.assign({}, BaseFilterBarDelegate);

    return EmployeeBenchReportFilterBarDelegate;
});
