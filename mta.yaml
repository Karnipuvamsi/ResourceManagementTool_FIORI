_schema-version: "3.2"
ID: glassboard
description: Generated by Fiori Tools
version: 1.0.1
modules:
- name: glassboard-destination-content
  type: com.sap.application.content
  requires:
  - name: glassboard-destination-service
    parameters:
      content-target: true
  - name: glassboard-repo-host
    parameters:
      service-key:
        name: glassboard-repo-host-key
  - name: glassboard-uaa
    parameters:
      service-key:
        name: glassboard-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: glassboard_html_repo_host
          ServiceInstanceName: glassboard-html5-service
          ServiceKeyName: glassboard-repo-host-key
          sap.cloud.service: glassboard
        - Authentication: OAuth2UserTokenExchange
          Name: glassboard_uaa
          ServiceInstanceName: glassboard-xsuaa-service
          ServiceKeyName: glassboard-uaa-key
          sap.cloud.service: glassboard
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: glassboard-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: glassboard-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - glassboard.zip
      name: glassboard
      target-path: resources/
- name: glassboard
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: glassboard-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: glassboard-destination-service
    service-plan: lite
- name: glassboard-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: glassboard-xsuaa-service
    service-plan: application
- name: glassboard-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: glassboard-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
